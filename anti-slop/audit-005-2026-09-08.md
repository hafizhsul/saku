# Ganti layar budgets → bottom-sheet Alokasi — 2026-09-08 (audit-005)

Permintaan: menu budgets dihilangkan; alokasi seperti modal Stitch.
Rute `app/budgets.tsx` + `app/budget-form.tsx` diarsip ke `attic/` (git mv,
bukan hapus) + dikecualikan dari tsconfig/eslint; Stack.Screen dicabut.
Data (provider/storage) tidak berubah.

## Sheet Alokasi (`src/components/AlokasiSheet.tsx`)
- Bottom sheet: handle, judul + X, ringkasan Total Terdistribusi vs Sisa
  Belum Dialokasikan (merah bila minus), slider per kategori, chip tambah
  kategori, footer Batal/Simpan (alasan: satu layar untuk semua aksi budget).
- Slider custom PanResponder (tanpa dep native): ketuk/geser, min 0
  step 50rb maks 3jt, hit area 44px (R-03), web + native.
- Nol saat simpan = hapus (skema hanya positif); best-effort tanpa gagal login.
- Beranda: tombol Alokasi + "Semua" membuka sheet (bukan route).

## Verifikasi
- Typecheck PASS, lint PASS, unit 96/96, server 27/27.
- e2e `budgets.spec` ditulis ulang (tambah→geser→simpan→persist→hapus),
  lolos 2/2. Penuh: MENYUSUL (background).

## Penyelarasan modal ke Stitch (2026-09-08)
- Header ikon tune; badge % share + hint kategori; min/max caption slider;
  summary divider (total gelap 15px, sisa aksen); nilai baris aksen 16px.
- Screenshot vs Stitch: cocok (sisa merah negatif = data seed tanpa income, rumus benar).

## Delivery Gate: PASS
- Typecheck PASS, lint PASS, unit 96/96, server 27/27, e2e 18/18.
